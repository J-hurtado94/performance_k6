# azure-pipelines.yml
trigger: none

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: |
      docker run --rm \
        -v $(System.DefaultWorkingDirectory):/work \
        -w /work \
        grafana/k6:latest run test.js
    displayName: 'Ejecutar k6 con Docker'