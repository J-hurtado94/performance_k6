trigger: none

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    echo "Ejecutando ZAP Baseline..."
    docker run --rm -t ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t https://globalmvm.com/
  displayName: "Run ZAP Baseline"